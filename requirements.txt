Flask==3.0.0
Werkzeug==3.0.1

# Authentication
PyJWT==2.8.0
requests==2.31.0

# LangChain and LangGraph - pinned for compatibility
langchain-core==0.3.29
langchain==0.3.13
langgraph==0.2.62
langchain-community==0.3.13

# Databricks LangChain Integration - use latest
databricks-langchain==0.2.0

# Databricks SDK (required for personalization engine and vector search)
# Pinned to the version pip already resolves to — an open-ended >= here forces
# pip to re-search the whole dependency graph on every deploy as new upstream
# releases land, which is what caused the multi-minute "backtracking" installs.
databricks-sdk==0.122.0
databricks-vectorsearch==0.66

# Model Context Protocol (MCP) for vector store abstraction
mcp==1.19.0

# Vector Store - Pinecone (optional with MCP)
pinecone==9.1.0

# Data models and validation
pydantic>=2.0.0

# Environment management
python-dotenv>=1.0.0

# OpenAI for Databricks-compatible API calls
openai==2.47.0

pytest==9.1.1
